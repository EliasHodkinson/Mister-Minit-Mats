{
  "name": "mister-minit-mat-builder",
  "private": true,
  "version": "1.0.0",
  "description": "Guided price-mat brief builder for Mister Minit stores – emails the brief via Microsoft Graph",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  }
}
